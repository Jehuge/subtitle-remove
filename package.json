{
  "name": "subtitle-remove",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd front && npm run dev",
    "build": "cd front && npm run build",
    "preview": "cd front && npm run preview",
    "tauri": "export PATH=\"$HOME/.cargo/bin:$PATH\" && cd tauri && tauri",
    "tauri:dev": "export PATH=\"$HOME/.cargo/bin:$PATH\" && cd tauri && tauri dev",
    "tauri:build": "export PATH=\"$HOME/.cargo/bin:$PATH\" && cd tauri && tauri build",
    "install:front": "cd front && npm install",
    "front:dev": "cd front && npm run dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
